import { IonCard, IonContent, IonHeader, IonPage, IonTitle, IonToolbar } from '@ionic/react';
import React from 'react';

const DevicesCard: React.FC = () => {

    return (
        <IonCard className='flex'>
            <img></img>

        </IonCard>
    );
};

export default DevicesCard;